services:
  - type: web
    name: freefire-ban-checker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: DISCORD_BOT_TOKEN
        sync: false
    healthCheckPath: /health
    autoDeploy: true
